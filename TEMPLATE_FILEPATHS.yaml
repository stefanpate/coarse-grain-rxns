root: /path/to/repo  # Modify this
data: ${.root}/data
artifacts: ${.root}/artifacts
configs: ${.root}/configs
raw_data: ${.data}/raw
processed_data: ${.data}/processed
interim_data: ${.data}/interim
mappings: ${.raw_data}/mappings
known: ${.raw_data}/known
rules_dir: ${.processed_data}/rules
expansion_metrics: ${.processed_data}/expansions
mlruns: ${.artifacts}/mlruns
hpo_studies: ${.artifacts}/hpo_studies
coreactants: ${.artifacts}/coreactants
starters: ${.artifacts}/starters
rules: ${.artifacts}/rules
figures: ${.artifacts}/figures
mechinformed_mapped_rxns: ${.mappings}/mapped_known_reactions_x_mechinformed_rules.parquet
rc_plus_0_mapped_rxns: ${.mappings}/mapped_known_reactions_x_rc_plus_0_rules.parquet