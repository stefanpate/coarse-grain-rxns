defaults:
  - filepaths: filepaths
  - _self_

mapped_rxns_fn: mapped_sprhea_240310_v3_no_subunits_x_rc_plus_2_rules.parquet

hydra:
  run:
    dir: ${filepaths.processed_data}
  job:
    chdir: True
  sweep:
    dir: ${filepaths.processed_data}
    subdir: .